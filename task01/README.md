# День 01 — Запрос к Anthropic API

Отправляет сообщение в Claude через Anthropic API и выводит ответ в консоль.

## Запуск

```bash
cp ../.env.example ../.env  # заполни своим API ключом
go run . -message "Твоё сообщение"
```

## Демо

![demo](demo.gif)
